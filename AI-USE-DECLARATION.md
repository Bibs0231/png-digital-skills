# AI Use Declaration

## Assessment

**ISO229 Web Design - Assessment 3**  
**Responsive HTML5 + CSS3 Website**

## Student details

- Name: **Bill Junior Lagani**
- Student ID: **25576687**
- GitHub username: **Bibs0231**

## AI assistance used

ChatGPT was used as a learning and development assistant during
Assessments 2 and 3 of the PNG Digital Skills Hub project.

During Assessment 2, ChatGPT assisted with interpreting the assessment
requirements, developing the initial PNG Digital Skills Hub concept,
drafting website content, suggesting semantic HTML structure, basic CSS,
the learning-path SVG, README documentation, learning materials and
source/HTML checking guidance.

The initial Assessment 2 project therefore contained substantial
AI-assisted content and code.

During Assessment 3, ChatGPT provided guidance and example code for:

- organising CSS using custom properties;
- improving colour, typography and spacing consistency;
- implementing Flexbox for navigation and header alignment;
- implementing CSS Grid for the learning-path content;
- implementing responsive CSS Grid layouts for course cards;
- creating mobile-first responsive layouts;
- adding tablet and desktop media-query breakpoints;
- improving responsive form layout;
- maintaining responsive images and tables;
- improving visible focus states and accessibility;
- planning mobile, tablet and desktop testing;
- preparing responsive testing evidence; and
- updating README and submission documentation.

## Student contribution

I reviewed the suggested changes and implemented the Assessment 3
updates in Visual Studio Code.

I tested each change in the browser before committing it to GitHub and
used multiple Git commits to show progressive development from the
Assessment 2 website to the Assessment 3 responsive version.

### Design and development decisions I implemented

I implemented and reviewed:

- a reusable CSS colour and spacing system using custom properties;
- mobile-first responsive styling;
- Flexbox navigation and larger-screen header alignment;
- responsive learning-path Grid layout;
- responsive course-card Grid layouts;
- one-column mobile, two-column tablet and three-column desktop
  course layouts;
- responsive form layouts;
- responsive table behaviour;
- button, card, fieldset and panel styling;
- flexible images;
- media queries for larger viewports; and
- keyboard-visible focus styling.

I retained the existing semantic HTML structure and accessibility
features where they continued to meet the requirements.

## Testing I personally performed

I personally tested the website at the following representative
viewport widths:

- **375px** - mobile;
- **768px** - tablet; and
- **1440px** - desktop.

I checked that:

- navigation remained usable;
- text remained readable;
- page-level horizontal scrolling did not occur;
- course cards changed correctly between one, two and three columns;
- the learning-path layout adapted correctly;
- the form changed correctly between mobile and larger-screen layouts;
- responsive media remained within its container; and
- the comparison table remained usable on smaller screens.

I also tested the website using:

- Google Chrome; and
- Microsoft Edge.

## Accessibility testing I personally performed

I tested keyboard navigation using the Tab key without relying on the
mouse.

I confirmed that:

- navigation links could be reached;
- form fields followed a logical focus order;
- radio buttons and checkboxes could be operated;
- the textarea was reachable;
- the acknowledgement checkbox was reachable;
- the submit button was reachable; and
- visible focus styling appeared during keyboard navigation.

## Form validation testing I personally performed

I tested the practice enquiry form and confirmed that:

- required fields prevent empty submission;
- invalid email input is rejected;
- the number of learners is restricted to the permitted range;
- the message field enforces its minimum-length requirement;
- the practice acknowledgement is required; and
- a valid practice submission works as expected.

Testing results are documented in:

`evidence/A3-RESPONSIVE-TESTS.md`

## Validation and publication

I reviewed the final HTML and CSS validation results and saved the
supporting evidence in the `evidence/` folder.

I also checked the published GitHub Pages website in a private/incognito
browser window to confirm that the website is publicly accessible and
that navigation works.

## How I verified that I understand the submitted code

I reviewed the submitted HTML and CSS and can explain the purpose of the
main techniques used.

I can explain:

- why semantic elements such as `header`, `nav`, `main`, `section`,
  `article` and `footer` are used;
- how relative links connect the three pages;
- how `aria-current` identifies the current page;
- how `label for` connects to an input `id`;
- why different HTML input types are used;
- how HTML validation attributes such as `required`, `min`, `max`,
  `minlength` and `maxlength` affect browser behaviour;
- how CSS custom properties provide reusable design values;
- why Flexbox is suitable for one-dimensional layout such as navigation;
- why CSS Grid is suitable for multi-column card layouts;
- how `@media` queries change the layout at larger viewport widths;
- how mobile-first design starts with a simple small-screen layout and
  adds larger-screen enhancements;
- how `max-width: 100%` prevents images from overflowing their
  containers;
- how responsive form fields move between one-column and two-column
  layouts; and
- how visible focus styling supports keyboard accessibility.

I can locate and modify these parts of the website in Visual Studio
Code and explain the changes made during Assessment 3.

## Declaration

I confirm that AI assistance has been disclosed accurately. I reviewed,
implemented, tested and documented the submitted website and understand
the code and design decisions used in the final Assessment 3 project.

Date: **26-Sep-2026**
